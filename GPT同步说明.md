# 错题助手开发同步说明

这是一个纯静态网页项目，用来导入题库、生成试卷、整理错题、保存文件夹，并支持数学公式渲染。项目不依赖后端，用户数据保存在自己的浏览器 `localStorage` 中。

## 当前部署文件

- `index.html`：页面结构，引用 KaTeX、`style.css` 和 `app.js`
- `style.css`：界面样式、移动端适配、公式显示样式
- `app.js`：题库、粘贴导入、公式修复、组卷、错题本、文件夹、最近删除、备份、打印等逻辑
- `README.md`：公开部署和使用说明
- `.gitignore`：忽略本地无关文件

## 当前核心功能

- 手动添加题目
- JSON / CSV 文件导入
- 粘贴导入，并进入导入预览后再确认导入
- 自动识别题目、选项、答案、解析、科目、章节、难度、题型
- KaTeX 数学公式渲染
- 自动修正常见数学公式写法
- 一键修复已有题库公式
- 搜索、筛选、批量打标签、批量改掌握状态
- 生成整张试卷、错题卷、复习卷、筛选结果组卷
- 错题本、错题次数、掌握状态
- 最近题目与最近题目批量打标签
- 最近题目只保留最近一次导入的内容，方便把本次导入单独组卷
- 文件夹保存试卷和错题本
- 左侧栏可收起，题库支持批量删除
- 删除题目会进入最近删除，可恢复、彻底删除或一键清空
- 导入和修复公式时会自动清理完全或高度相似的重复题，保留内容更完整的一道
- 导出备份、恢复备份
- 打印、导出 Word、导出 PDF

## 数据安全

当前版本数据只保存在当前浏览器。换设备、换浏览器、无痕模式或清理浏览器数据，都可能导致看不到原来的题库。使用前后建议定期点击“导出备份”。

## 给 GPT 的协作提示

继续开发时请保持纯静态前端，不要默认接入后端、账号或数据库。除非用户明确要求云同步，否则优先保证本地保存、备份恢复、粘贴导入、数学公式渲染和错题组卷稳定。
