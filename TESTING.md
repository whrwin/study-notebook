# 发布前测试清单

## 1. 粘贴导入选择题

复制一题包含【题目】【选项】【答案】【解析】的选择题，点击“粘贴导入”。预期：出现导入预览，选项 A/B/C/D 正常分开，确认导入后进入题库。

## 2. 粘贴导入计算题

复制一题没有选项的计算题，选项写“无”。预期：导入预览中没有选项列表，答案和解析正常显示。

## 3. 公式渲染

导入包含 `x_1\le \bar{x}_1`、`\frac{p_1}{p_2}`、`f(x_1,x_2)=\min{x_1,x_2}` 的题目。预期：预览和题库中都渲染为正常数学公式。

## 4. 错题标记

在题库中点击“标为错题”。预期：题目出现错题标记，并进入错题卷页面。

## 5. 生成错题卷

至少标记两道错题后点击“生成全部错题卷”。预期：当前试卷只包含错题。

## 6. 筛选组卷

给部分题目打标签，再按标签筛选并点击“用筛选结果组卷”。预期：试卷只包含筛选出的题目。

## 7. 备份导出

点击“导出备份”。预期：浏览器下载一个 `.json` 备份文件，文件中包含题库、错题、文件夹和复习记录。

## 8. 备份恢复

选择刚导出的备份文件恢复。预期：题库数量、错题标记、文件夹数据恢复正常。

## 9. 图片插入

编辑一道题，选择图片插入到题目或解析。预期：保存后图片显示在题目卡片中，且题目仍可导出备份。

## 10. 打印 / 导出

生成试卷后点击打印、导出 Word、导出 PDF。预期：页面进入打印或下载流程，题目、答案、解析和公式显示正常。

## 11. 最近题目组卷

一次粘贴导入 3 道题并确认导入。预期：“最近题目”只显示这 3 道题，点击“最近题目组卷”后当前试卷也只包含这 3 道题。

## 12. 批量删除与恢复

在题库中勾选多道题，点击“批量删除”。预期：题库数量减少，题目进入“最近删除”；在最近删除中点击“恢复”后题目回到题库。

## 13. 重复题自动清理

连续导入两道题干和答案几乎相同、但解析长短不同的题。预期：系统自动移除字数更少的重复题，并能在“最近删除”中看到它。

## 14. 左侧栏收起

点击顶部“收起左侧栏”。预期：左侧面板隐藏，右侧题目区域占满宽度；再次点击可恢复左侧栏。
